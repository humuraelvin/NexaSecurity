'use client';

import { NetworkMap } from '@/services/networkService';

interface Props {
  data: NetworkMap;
}

export default function NetworkGraph({ data }: Props) {
  // Implement your network graph visualization here
  return (
    <div>
      {/* Graph implementation */}
    </div>
  );
} 